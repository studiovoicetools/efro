services:
  - type: web
    name: efro-prod
    env: node
    branch: main
    plan: starter
    buildCommand: |
      npm ci || npm install
      npm run build
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 20.19.5
      - key: NODE_ENV
        value: production
